# Security policy

Report security issues privately through GitHub's security advisory feature when enabled. Do not include real resumes, credentials or applicant data in public issues.

API keys must be supplied through environment variables. CareerFlow never writes the key to its workspace. Keep the runtime workspace on a device and filesystem appropriate for the sensitivity of employment data.
